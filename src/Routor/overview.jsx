// Import necessary modules from React
import React from "react";

// Define a functional component called over
const over= ()=> {
// Return the following JSX code
return(
<>
<h1>hello i m overview page</h1>
</>
// Close the JSX code
)

};
// Export the over component as the default export
export default over;
